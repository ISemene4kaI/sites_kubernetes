replicaCount: 1

image:
  repository: ghcr.io/isemene4kai/semka-informatics
  tag: "0caaec0a47941e8b0fdbe781c2777907dd773e85"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

serviceAccount:
  create: true
  name: ""
  automountServiceAccountToken: false

podSecurityContext: {}

containerSecurityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  seccompProfile:
    type: RuntimeDefault

namespace: semka-informatics

ingress:
  enabled: true
  className: traefik
  host: isemene4kai.ru
  path: /
  pathType: Prefix

  tls:
    secretName: isemene4kai-ru-tls

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 512Mi

env:
  APP_HOST: "0.0.0.0"
  APP_PORT: "8000"
  FILES_DIR: "/app/app/code_storage"
  STATIC_DIR: "/app/app/static"
  TEMPLATES_DIR: "/app/app/templates"
  VIEWS_FILE: "/data/views.json"

secretEnv: {}

persistence:
  enabled: true
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  size: 1Gi
  mountPath: /data